---
title: "Общая схема работы"
sidebarTitle: "Общая схема"
description: "Описание общего процесса взаимодействия при проведении платежей банковскими картами"
---

```mermaid
sequenceDiagram

autonumber

    participant site as Ваш сайт #lightGreen

    participant server as Ваш сервер

    participant finmar as Finmar API

    site->>server: Пользователь нажимает кнопку метода

    server->>finmar: Запрос на получение ссылки на платёжную страницу

    finmar-->>server: Ссылка на платёжную страницу

    server-->>site: Показ пользователю платёжной страницы

    note over site:  Пользователь проходит платёжный флоу

    finmar-->>site: Перенаправление пользователя на `url_redirect_success` или `url_redirect_fail`

    finmar-->>server: Обратное уведомление

    server-->>site: Обработка операции (зачисление средств пользователю)

    server->>finmar: Подтверждение операции    

    loop Запрос успешных неподтверждённых операций

            server->>finmar: GET /collector/api/v3/client_unconfirmed_success_payments

            finmar-->>server: array()

        note over server: Обработка операций

        server -->> site: Обработка операции

        server -->>site: Обновление баланса

        server->>Finmar: Подтверждение операции        

    end
```

<Steps>
  <Step title="Инициирование оплаты пользователем">
    Пользователь нажимает кнопку оплаты на вашем сайте, инициируя процесс взаимодействия с вашим сервером.
  </Step>
  <Step title="Запрос ссылки на платёжную страницу">
    Ваш сервер отправляет запрос к API Finmar для получения уникальной ссылки на платёжную страницу.
  </Step>
  <Step title="Получение ссылки на платёжную страницу">
    API Finmar взаимодействует с платёжным провайдером и передаёт вашему серверу сгенерированную ссылку на платёжную страницу.
  </Step>
  <Step title="Показ пользователю платёжной страницы">
    Ваш сервер перенаправляет пользователя на полученную платёжную страницу, где он проходит процесс оплаты.
  </Step>
  <Step title="Перенаправление пользователя на `url_redirect_success` или `url_redirect_fail`">
    После завершения платежа платёжный провайдер перенаправляет пользователя обратно на ваш сайт по заранее определённым URL-адресам (`url_success` или `url_result`).
  </Step>
  <Step title="Обратное уведомление">
    API Finmar отправляет уведомление на ваш сервер о статусе операции через указанный `url_callback`.
  </Step>
  <Step title="Обработка операции (зачисление средств пользователю)">
    Ваш сервер обрабатывает полученное уведомление, проверяет подпись, зачисляет средства пользователю и подтверждает получение уведомления, отправляя запрос `POST /api/operation/set_ack_id` в Finmar.
  </Step>
  <Step title="Подтверждение операции">
    После успешной обработки операции (зачисления средств пользователю) ваш сервер подтверждает обработку операции, отправляя запрос на подтверждение в Finmar API.
  </Step>
  <Step title="Обработка новых успешных операций (цикл)">
    Ваш сервер периодически запрашивает у Finmar список неподтверждённых успешных операций (`GET /collector/api/v3/client_unconfirmed_success_payments`). После получения и обработки этих операций ваш сервер подтверждает обработку каждой операции, обновляет баланс пользователя на вашем сайте и снова устанавливает `operation_ack_id` для предотвращения дублирования. Этот цикл повторяется для обработки всех новых успешных операций.
  </Step>
</Steps>

<Note>
  Перед началом интеграции убедитесь, что вы получили необходимые учетные данные и настроили URL-адреса для обратных вызовов.
</Note>
